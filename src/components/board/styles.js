import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  board: {
    borderWidth: 1,
    borderColor: '#fff'
  },
  row: {
    flexDirection: 'row',
  },
});

